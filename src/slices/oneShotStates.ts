import { createSlice } from "@reduxjs/toolkit";

const oneShotStatesSlice = createSlice({
  name: 'oneShotStates',
  initialState: {},
  reducers: {
    
  }
})

// TODO: use OneShotState from OneShotState.ts

export const {} = oneShotStatesSlice.actions;

export default oneShotStatesSlice.reducer;
